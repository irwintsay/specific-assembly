# Specific Assembly

### About

### Technologies Used
* Node/Express
* PostgreSQL
* EJS Templating

### Install
* `npm install`
* Create a `.env` file for environmental variables
* Create a new postgresql database called `specific_assembly`
* Run the `migrations/schema.sql` file: `$ psql -d specific_assembly -f migrations/schema.sql`

